import sys

N = int(sys.stdin.readline())

def countnumber(N, val):
    if N < 10:
        return 1
    else:
        cnt = 0
        
        return cnt + countnumber(N // 10, val)
    

ans = []
for i in range(10):
    ans.append(countnumber(N, i))
    print(ans[i], end=' ')
