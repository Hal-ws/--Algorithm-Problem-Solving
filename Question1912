N = int(input())
series = list(map(int, input().split()))

maxVal = [series[N - 1]]

for i in range(2, N):
    if maxVal[i - 2] > 0:
        maxVal.append(maxVal[i - 2] + series[i])
    else:
        maxVal.append(series[i])
    
